# Routes
# This file defines stories application routes (Higher priority routes first)
# ~~~~

# Stories
GET     /stories/json               controllers.StoryController.allJson

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

# Misc
# TODO Redirect stories other pages here
GET     /pageNotFound               controllers.Application.pageNotFound
